# Cloudflare Pages 快速部署指南

## 🚀 部署步骤

### 方法一: 通过 Cloudflare Pages Dashboard (推荐)

#### 1. 准备构建产物
```bash
# 已完成构建,产物在 dist/ 目录
npm run build
```

#### 2. 登录 Cloudflare Pages
1. 访问: https://dash.cloudflare.com/
2. 进入 **Workers & Pages**
3. 点击 **Create application** → **Pages** → **Upload assets**

#### 3. 上传构建产物
1. 点击 **Upload assets**
2. 创建项目名称 (例如: `api-cost-tool`)
3. 将 `dist/` 目录下的所有文件拖拽上传:
   - `index.html`
   - `assets/` 文件夹中的所有文件
   - `favicon.svg` (如果有)

#### 4. 部署
1. 点击 **Deploy site**
2. 等待部署完成 (通常 10-30 秒)
3. 获得访问链接: `https://your-project.pages.dev`

---

### 方法二: 通过 Git 集成 (适合后续更新)

#### 1. 推送代码到 Git 仓库
```bash
git add .
git commit -m "feat: 添加智能粘贴识别功能"
git push
```

#### 2. 在 Cloudflare Pages 创建项目
1. 进入 **Workers & Pages** → **Create application** → **Pages**
2. 选择 **Connect to Git**
3. 选择你的 Git 仓库

#### 3. 配置构建设置
```yaml
构建命令: npm run build
构建输出目录: dist
根目录: /
Node 版本: 18 或更高
```

#### 4. 环境变量 (可选)
无需额外环境变量

#### 5. 部署
点击 **Save and Deploy**,等待自动构建和部署

---

## 📦 构建产物说明

当前构建产物 (dist/):
```
dist/
├── index.html                      (0.79 kB)
└── assets/
    ├── index-ByMUQCDk.css         (20.42 kB)
    ├── index-BlOpaFzZ.js          (35.46 kB)
    └── vue-vendor-B4ZIvYVF.js     (71.71 kB)

总大小: ~128 kB
Gzip 后: ~46 kB
```

---

## ✅ 验证部署

部署成功后,访问你的 Cloudflare Pages 链接,验证以下功能:

### 1. 基础功能
- ✅ 页面正常加载
- ✅ API 配置表单显示正常
- ✅ 预设平台按钮可点击

### 2. 智能粘贴识别功能
- ✅ 顶部有蓝紫渐变的**「一键智能识别」**区域
- ✅ 粘贴文本后自动显示识别结果
- ✅ 点击**「应用到下方表单」**能填充数据

### 3. 测试案例
粘贴以下文本到智能识别框:
```
Base URL:https://kiro2api-node.zeabur.app
key:sk-hdushdgsg988hfuhftte6bbst5rwvv
协议:Anthropic
```

预期结果:
- 📍 识别出 URL: `https://kiro2api-node.zeabur.app`
- 🔑 识别出 Key: `sk-hdushdg...t5rwvv` (脱敏显示)

---

## 🔧 自定义域名 (可选)

### 1. 在 Cloudflare Pages 设置自定义域名
1. 进入项目设置 → **Custom domains**
2. 点击 **Set up a custom domain**
3. 输入你的域名 (例如: `api-cost.example.com`)
4. 按照提示配置 DNS 记录

### 2. DNS 配置
```
类型: CNAME
名称: api-cost (或其他子域名)
内容: your-project.pages.dev
代理状态: 已代理 (橙色云)
```

---

## 🚨 常见问题

### Q1: 上传后页面空白?
**A:** 检查构建产物是否正确:
```bash
# 重新构建
npm run build

# 检查 dist/ 目录
ls -la dist/
```

### Q2: 智能识别功能不工作?
**A:** 清除浏览器缓存后刷新,或使用无痕模式测试

### Q3: 如何更新部署?
**A:**
- 方法一 (手动): 重新上传 dist/ 目录
- 方法二 (Git): 推送代码后自动重新部署

### Q4: 部署后样式错乱?
**A:** 检查 Vite 配置的 base 路径是否正确 (默认为 `/`)

---

## 📊 性能优化建议

当前已优化:
- ✅ Gzip 压缩 (自动)
- ✅ 资源 hash 命名 (自动)
- ✅ 代码分割 (自动)

Cloudflare Pages 自动提供:
- ✅ 全球 CDN 加速
- ✅ HTTP/2 支持
- ✅ 自动 HTTPS
- ✅ DDoS 防护

---

## 🎉 部署完成!

部署成功后,你的智能粘贴识别功能将在全球范围内可用!

**下一步:**
1. 分享链接给团队成员
2. 收集用户反馈
3. 根据需要添加更多功能

**文档链接:**
- 使用指南: [SMART_PASTE_GUIDE.md](SMART_PASTE_GUIDE.md)
- 功能对比: [FEATURE_COMPARISON.md](FEATURE_COMPARISON.md)

---

## 📝 快速命令参考

```bash
# 开发环境
npm run dev

# 构建生产版本
npm run build

# 预览构建产物
npm run preview

# 运行测试
node test/smartParse.test.js
```
